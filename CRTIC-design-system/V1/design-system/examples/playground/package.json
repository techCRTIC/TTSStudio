{
  "name": "crtic-ds-playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "comment": "Deps are hoisted to design-system/ (single React, shared with the kit source). Run from design-system/: npm run playground.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  }
}
